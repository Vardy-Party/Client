<?xml version="1.0" encoding="utf-8"?>
<!-- Full-screen splash (logo + build lines). Phone SplashActivity paints this
     before MAUI starts. TV MainActivity still uses it as windowBackground. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_screen_background" />
    <item android:gravity="center">
        <bitmap
            android:gravity="center"
            android:src="@drawable/vardyparty_splash_generated" />
    </item>
</layer-list>
