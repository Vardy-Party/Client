<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application 
        android:allowBackup="true" 
        android:label="VardyParty"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:icon="@mipmap/appicon"
        android:roundIcon="@mipmap/appicon_round"
        android:logo="@mipmap/appicon"
        android:banner="@drawable/banner">
        <!-- Phone drawer: SplashActivity. TV row: this alias. Do not leave
             LEANBACK_LAUNCHER enabled on phones — several launchers then
             hide the package from the app drawer after adb install. -->
        <activity
            android:name="com.vardyparty.SplashActivity"
            android:enabled="@bool/show_phone_launcher"
            tools:node="merge" />
        <activity-alias
            android:name="com.vardyparty.TvLeanbackAlias"
            android:targetActivity="com.vardyparty.MainActivity"
            android:enabled="@bool/show_tv_launcher"
            android:exported="true"
            android:label="VardyParty"
            android:banner="@drawable/banner">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity-alias>
    </application>
    
    <!-- Network permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- Android TV support -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    
    <!-- Video playback features -->
    <uses-feature android:name="android.hardware.screen.landscape" android:required="false" />

    <!-- Required for Auth0 Chrome Custom Tabs on Android 11+ (API 30+) -->
    <queries>
        <intent>
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>
    </queries>
</manifest>