# Copy to .env (gitignored) in the repo root.
# Sync signing secrets: pwsh scripts/sync-github-secrets-from-env.ps1
#
# WINDOWS_CERT_BASE64=
# WINDOWS_CERT_PASSWORD=
#
# Linux snap minisign (hashed ED). Base64 of keyId[8] + seed[32].
# MINISIGN_SECRET_KEY=
